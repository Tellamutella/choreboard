// function submitTask(submissionHTML) {
//   const state = document.getElementById("task-submission-state");
//   // console.log(state);
//   // console.log(state.innerHTML);
//   state.innerHTML = submissionHTML;
// }

// <% unless @task_submission.errors.any? %>
//   submitTask("(Submitted, current state: <%= @task_submission.state %>)");
// <% end %>
